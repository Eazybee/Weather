import { css } from 'styled-components';

const styles = css`
  padding: 1rem 5rem;

  .foot {
    padding: 1rem 0;
    margin-top: 4rem;
  }

  @media screen and (max-width: 1000px) {
    padding: 0 1rem;
  }
`;

export default styles;
